# PRD：SBTI 仿制 App（Web 版，可模块化复用）

- 版本：v0.2（草案）
- 日期：2026-04-11
- 状态：待你评审/修改
- 参考产品：https://sbti.unun.dev/

## 1. 项目背景
原始站点是一个偏娱乐化的人格测试产品，核心是“31题答题流程 + 15维度评分 + 人格结果展示”。
本项目目标是在本地实现一个高相似度的 Web 版本，并将题库、得分标准、结果判定逻辑模块化沉淀，后续可复用于其他任意主题的心理测试产品。

## 2. 目标与非目标
### 2.1 目标（Goals）
1. 还原 SBTI 的核心体验：首页、答题、结果三段式流程。
2. 还原 SBTI 核心规则：31题流程、饮酒隐藏分支、15维度评分、人格匹配逻辑。
3. 抽离可复用测试引擎：题库层、评分层、结果层、展示层解耦。
4. 形成“数据驱动”的测试模板：新增一个测试主题时，尽量只替换配置数据，不改核心代码。
5. 保持纯前端本地运行（无需后端、无需注册登录）。

### 2.2 非目标（Non-Goals）
1. 不做心理学专业诊断背书。
2. 不做用户账户体系、云端存储、用户画像系统。
3. 不做支付与商业化能力。
4. 当前阶段不做 CMS 可视化配置后台。

## 3. 用户与使用场景
### 3.1 目标用户
1. 对娱乐型人格测试感兴趣的普通用户。
2. 希望快速体验、无需注册的用户。
3. 未来要复用该框架做其他测试主题的产品/运营同学。

### 3.2 核心场景
1. 用户进入首页，点击“开始测试”。
2. 用户按顺序完成题目作答。
3. 用户提交后查看人格结果与维度解释。
4. 用户可“重新测试”或“回到首页”。
5. 开发者可替换数据包并快速生成新测试。

## 4. 产品范围（MVP）
### 4.1 页面结构
1. 首页（Landing）
2. 答题页（Questionnaire）
3. 结果页（Result）

### 4.2 模块范围
1. `QuestionBank`：题目、选项、分值映射、题目顺序规则。
2. `FlowEngine`：题目可见性、特殊题插入、进度控制。
3. `ScoringEngine`：维度打分、分档（L/M/H）、向量生成。
4. `ResultEngine`：类型匹配、排序、特殊分支覆盖。
5. `Renderer`：页面渲染与交互。
6. `ThemePack`：文案、海报、配色、品牌信息。

### 4.3 本期交付边界
1. 首个 `ThemePack` 为 SBTI。
2. 支持通过替换数据包切换测试主题（至少具备接口与示例结构）。
3. 不要求本期完成多主题 UI 运营后台。

## 5. 详细需求
### 5.1 首页
1. 展示主标题与开始按钮。
2. 展示原作者说明文案。
3. 点击“开始测试”进入答题页并初始化会话。

### 5.2 答题页
1. 显示当前完成进度（done / total）。
2. 一次只展示当前未作答题目。
3. 用户选择选项后立即记录并进入下一题。
4. 未完成全部可见题目时，提交按钮禁用。
5. 支持“返回首页”。

### 5.3 题目流程逻辑（FlowEngine）
1. 常规题集合随机打乱。
2. `drink_gate_q1` 随机插入到常规题队列。
3. 若 `drink_gate_q1` 选择“饮酒（value=3）”，在其后插入 `drink_gate_q2`。
4. 否则不展示 `drink_gate_q2`。
5. 引擎支持通用“条件显示题”机制（为未来主题预留）。

### 5.4 结果计算逻辑（ScoringEngine + ResultEngine）
1. 仅常规题参与15维度打分（每维2题，分值累加）。
2. 分数映射：
   - `<=3 => L`
   - `==4 => M`
   - `>=5 => H`
3. 将15维 L/M/H 转为向量，与 `NORMAL_TYPES` 模板计算距离与相似度。
4. 常规人格排序规则：
   - 先按 `distance` 升序
   - 再按 `exact` 降序
   - 再按 `similarity` 降序
5. 特殊分支：
   - 若 `drink_gate_q2 == 2`：最终人格强制 `DRUNK`
   - 否则若最佳常规人格 `similarity < 60`：最终人格为 `HHHH`
   - 否则最终人格为最佳常规人格
6. 引擎支持通过配置扩展“覆盖规则”和“兜底规则”。

### 5.5 结果页（Renderer）
1. 展示最终人格：`code`、中文名、`intro`、`desc`。
2. 展示匹配度 badge 与系统备注。
3. 展示15维评分明细（维度名、L/M/H、原始分、解释）。
4. 展示友情提示与作者话术文案。
5. 支持“重新测试”和“回到首页”。

### 5.6 数据结构要求（可复用核心）
1. `question-set.json`：题目、选项、维度映射、特殊流程规则。
2. `dimension-set.json`：维度元信息与解释文案。
3. `type-set.json`：人格模板、文案、匹配模式。
4. `rule-set.json`：特殊触发、兜底策略、阈值配置。
5. 数据层与引擎层分离，避免把业务常量硬编码在页面组件。

## 6. 数据与依赖
1. 首期题库与人格库来源：`sbti-cli/src/bundled-data.mjs`
2. 海报资源来源：`sbti-cli/assets/type-images/*`
3. 海报名单：`sbti-cli/assets/type-images/manifest.json`
4. 运行形态：纯静态文件 + 本地 HTTP Server

## 7. 非功能需求
1. 性能：首屏加载 < 2s（本地环境）
2. 响应式：移动端可用（360px 宽度可操作）
3. 稳定性：完整答题流程不出现阻塞
4. 隐私：不上传答案，不持久化个人信息
5. 可维护性：核心引擎可单测，主题数据可独立替换

## 8. 验收标准（Acceptance Criteria）
1. 用户可完成从首页到结果页的完整闭环。
2. 未完成所有可见题目时无法提交。
3. 饮酒分支触发规则符合预期。
4. DRUNK 与 HHHH 触发规则符合预期。
5. 结果页能展示15维评分与解释。
6. 页面在桌面端与移动端均可正常使用。
7. 题库、规则、结果模板与渲染层解耦，代码结构可复用。
8. 通过替换一套主题数据，系统可运行“非 SBTI 主题”的最小示例流程。

## 9. 里程碑（建议）
1. M1：SBTI 页面骨架与交互闭环（0.5 天）
2. M2：流程引擎抽离（题目流转、分支逻辑）（0.5 天）
3. M3：评分与结果引擎抽离（0.5 天）
4. M4：主题数据结构标准化与示例包（0.5 天）
5. M5：联调、验收、文档沉淀（0.5 天）

## 10. 待你确认项
1. 多主题复用优先级：是否本期必须交付“第二个主题示例”？ 不需要
2. 模块化颗粒度：你更倾向按“文件级抽离”还是“npm package 级抽离”？ 文件级抽离
3. 数据格式：最终使用 `json`、`js module`，还是两者并存？ json
4. 是否需要增加“结果分享图”作为通用模块能力？ 需要
5. 是否需要同步部署到公开域名（Cloudflare Pages / Vercel）？ 不需要
